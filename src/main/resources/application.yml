server:
  port: 9001

#应用名称及验证账号
spring:
  application:
    name: api-passenger
  cloud:
    service-registry:
      auto-registration:
        enabled: true

eureka:
  client:
    #设置服务注册中心的URL
    service-url:
      defaultZone: http://root:root@eureka-7901:7901/eureka/
  instance:
    hostname: localhost
    instance-id: api-passenger